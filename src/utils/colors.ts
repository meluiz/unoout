import { createColors } from 'picocolors'

/**
 * Creates and returns a colors object.
 *
 * @param {boolean} enableFormatting - Whether to enable formatting.
 * @returns {object} - The colors object.
 */
export const colors = createColors(true)
